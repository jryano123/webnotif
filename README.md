# Web Notification System (webnotif)

This is a SvelteKit project that implements a web-based notification system, likely using Shadcn Svelte's data table component to display notifications.

## Features
* Display notifications in a data table
* Filter and sort notifications
* Mark notifications as read/unread
* (Add any other specific features your project has or will have)

## Technologies Used
* SvelteKit
* Svelte
* Shadcn Svelte
* TypeScript
* (Add any database, API frameworks, etc., you are using)

## Setup and Development

Everything you need to build and run this SvelteKit project:

### Creating a Project
If you're seeing this, you've probably already done this step. Congrats!

```bash
# create a new project in the current directory
npx sv create

# create a new project in my-app
npx sv create my-app